version: "3.8"
services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    entrypoint: ["uvicorn", "aperture.main:app", "--reload", "--host", "0.0.0.0"]

    volumes:
      - .:/app:rw

    ports:
      - "8000:8000"

{% extends "base.jinja2" %}
{% block title %}Aperture: {{ provider.human_identifier }}{% endblock %}
{% block description %}Your identity challenge for {{ provider.human_identifier }}.{% endblock %}

{% block content %}
    <div class="content is-large is-size-5-mobile pt-5">
        <p>Your challenge code for <b>{{ provider.human_identifier }}</b> is:</p>
        <pre>{{ challenge }}</pre>
        <p>The URL for this challenge is:</p>
        <pre>{{ challenge_url }}</pre>
        <p>Write down the code and share the URL with the user you want to verify.</p>
        <p>Once they'll complete the verification, they will get a new URL proving the ownership of their account.</p>
    </div>
{% endblock %}
